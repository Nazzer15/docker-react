language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -f -t Nazzer15/docker-react Dockerfile.dev .

script:
  - docker run -e CI=true Nazzer15/docker-react npm run test -- --coverage





